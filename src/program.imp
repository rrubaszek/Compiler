# Binarna postać liczby 
PROGRAM IS
	a,b
BEGIN
    READ a;
    READ b;
    IF a < b THEN  
        WHILE a > 0 DO
            WRITE a;
            a := a - 1;
        ENDWHILE
    ELSE 
        WHILE b > 0 DO
            WRITE b;
            b := b - 1;
        ENDWHILE
    ENDIF
END
