#pragma once

enum Instructions {
    PROGRAM, 
    PROCEDURE, 
    IS, 
    BEGIN, 
    END,
    IF, 
    THEN, 
    ELSE, 
    ENDIF, 
    WHILE, 
    DO, 
    ENDWHILE,
    REPEAT, 
    UNTIL, 
    FOR, 
    FROM, 
    TO, 
    DO, 
    ENDFOR,
    DOWNTO,
    READ,
    WRITE,
    T
};
